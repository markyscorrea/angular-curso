import { NgelseDirective } from './ngelse.directive';

describe('NgelseDirective', () => {
  it('should create an instance', () => {
    const directive = new NgelseDirective();
    expect(directive).toBeTruthy();
  });
});
