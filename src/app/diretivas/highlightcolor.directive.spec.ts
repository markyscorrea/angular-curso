import { HighlightcolorDirective } from './highlightcolor.directive';

describe('HighlightcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightcolorDirective();
    expect(directive).toBeTruthy();
  });
});
